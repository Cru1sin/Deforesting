# Figure manifest

| Figure | Intended conclusion | Evidence | Uncertainty | Source file | Export | Reviewer risk |
|---|---|---|---|---|---|---|
| Fig. 1 | Unsmoothed measurements produce auditable, policy-conditional empirical minima, but many minima lie in broad cost valleys. | Raw heating capacity, clean reference, renewal-cost curve, 47 complete-cycle optima and 50 observed tickets. | Point estimates inherit the fixed-ticket and clean-reference assumptions; no counterfactual early-defrost outcomes. | `scripts/analyze_raw_optimal_defrost.py`; `report/raw_optimal_defrost/source_data/` | Existing SVG/PDF/TIFF/PNG; remove in-figure title for manuscript version. | Readers may mistake a retrospective empirical minimum for a causal global optimum. |
| Fig. 2 | Candidate-level regret, rather than a universal time window, maps each image to pre-, near- or post-optimal state while preserving experiment-level isolation. | Regret curves at 1%, 2%, 5% and 10%; 105,178 metadata rows; 57,213 eligible candidate-domain images; experiment split table. | Low-regret sets can be disconnected; label width depends on the cost model and threshold. | `report/raw_optimal_defrost/source_data/candidate_cost_curves.parquet`; `report/rgb_cost_labels/` | Planned Python SVG/PDF/TIFF/PNG plus panel source tables. | A continuous shaded envelope could falsely label high-regret gaps as near-optimal. |
| Fig. 3 | Visual prediction is credible only to the extent that RGB exceeds a strong retrospective time-only control in identical held-out experiments. | Leave-one-experiment-out RGB, time-only and RGB+time predictions; paired experiment-level bootstrap; camera groups. | The time-progress control uses the future observed cycle boundary and is diagnostic, not deployable; cohort extraction is in progress. | `scripts/evaluate_rgb_feature_shards.py`; `report/rgb_full_cohort/` | Planned Python SVG/PDF/TIFF/PNG plus source CSVs. | High image-level accuracy may arise from temporal, illumination or experiment drift. |
| Fig. 4 | Generalization is heterogeneous across experiments and errors must be judged by their associated cost regret, not accuracy alone. | Per-experiment balanced accuracy, class-balanced misclassification regret, confusion patterns and selected failure cycles. | Existing test data were inspected during development; all-experiment LOEO is developmental rather than a blind confirmatory test. | `report/rgb_full_cohort/experiment_metrics.csv`; `predictions.parquet` | Planned Python SVG/PDF/TIFF/PNG plus source CSVs. | Pooling frames would create artificially narrow uncertainty and conceal failing experiments. |
